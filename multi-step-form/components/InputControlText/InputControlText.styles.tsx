import { StyleSheet } from 'react-native';
import { Outlines, Sizing } from 'ui/theme/styles';


export const styles = StyleSheet.create({
    inner: {
        flexDirection: 'row',
        alignItems: 'center',
    },
});